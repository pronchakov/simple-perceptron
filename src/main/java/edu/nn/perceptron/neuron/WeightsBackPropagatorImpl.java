package edu.nn.perceptron.neuron;

import edu.nn.perceptron.Layer;

public class WeightsBackPropagatorImpl<OUTPUT> implements WeightsBackPropagator<OUTPUT> {
    @Override
    public void propagateWeights(Layer<OUTPUT> fromLayer) {

    }
}
